# Database
DATABASE_URL=postgres://user:pass@host/db?sslmode=require

# JWT
JWT_SECRET=change-me

# Telegram bot (login / registration)
TELEGRAM_BOT_TOKEN=
# Optional if bot username cannot be resolved at startup
TELEGRAM_BOT_USERNAME=YourBotName

# SMTP (legacy email auth — kept for fallback)
SMTP_HOST=smtp.yandex.ru
SMTP_PORT=465
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=

# Cloudflare R2 (S3-compatible API)
# Dashboard: R2 → Manage R2 API Tokens → Create API token
S3_BUCKET=your-bucket-name
AWS_ACCESS_KEY_ID=your-r2-access-key-id
AWS_SECRET_ACCESS_KEY=your-r2-secret-access-key
AWS_REGION=auto
# Account ID из URL дашборда R2
S3_ENDPOINT=https://YOUR_ACCOUNT_ID.r2.cloudflarestorage.com
# Публичный URL: R2 → bucket → Settings → Public access (r2.dev) или свой домен
S3_PUBLIC_BASE_URL=https://pub-xxxx.r2.dev
# Обычно для R2 не нужно; оставьте false
S3_USE_PATH_STYLE=false
